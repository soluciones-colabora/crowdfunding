import { EmailAvailableDirective } from './email-available.directive';

describe('EmailAvailableDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailAvailableDirective();
    expect(directive).toBeTruthy();
  });
});
