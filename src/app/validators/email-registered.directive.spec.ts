import { EmailRegisteredDirective } from './email-Registered.directive';

describe('EmailRegisteredDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailRegisteredDirective();
    expect(directive).toBeTruthy();
  });
});
